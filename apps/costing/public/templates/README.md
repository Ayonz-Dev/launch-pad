# templates

The factory purchase-order export reads `ayonz-factory-po.xlsx` from this
directory. That binary template is a deploy-provisioned asset and is not
committed to git. Drop it in here to enable the PO export and its three tests.
